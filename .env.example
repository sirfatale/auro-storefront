# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Admin login now uses real Supabase Auth (email + password + MFA) instead of a
# client-side password. See the "Admin Access & MFA" section in README.md to
# create your admin user — no env var needed for it anymore.
